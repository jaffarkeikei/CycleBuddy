// CycleBuddy Contracts Library
// This is a placeholder file for the workspace

/// Simple utility function to verify the workspace is properly set up
pub fn is_workspace_setup() -> bool {
    true
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_workspace_setup() {
        assert!(is_workspace_setup());
    }
} 